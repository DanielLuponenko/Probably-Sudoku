# NumberClub core gameplay

This project currently ships the core Book-to-puzzle loop only. Keep changes
focused on gameplay, its persistence, and accessibility. Do not restore the
retired Club Shop, its 3D counter, Meshy cosmetics, or shop asset pipeline
without an explicit request.

Preserve existing work and rollback evidence. Never use destructive reset,
clean, force checkout, or force push.
