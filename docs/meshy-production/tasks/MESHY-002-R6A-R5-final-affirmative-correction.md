# MESHY-002-R6A-R5 — Final affirmative-prompt and enforcement correction

## Authority

- Binding master: `/Users/daniel/meshy_club_shop_master_prompt.md`
- Failed correction: `7ef2da499842fb171a97f670d1841dc15a62ac52`.
- Independent FAIL: R6A-R4 run `7B6FA66D-AE1F-407A-8264-CCDC21BBD4D7`, canonical `claude-sonnet-5`, result UUID `cbca0d2f-1c79-4d1a-9680-ec0c1a137bcf`.
- Correction run ID: `36C1899E-0221-495A-B0CC-978563BAFFCB`.
- Agent: `sonnet-meshy-pipeline-engineer`, requested model `sonnet`; no nested agents.
- Worktree: `/Users/daniel/.codex/worktrees/NumberClub/KAN-153-meshy-002-leafspec`.
- Branch: `feature/KAN-153-meshy-leafspec`.
- Exact parent: `7ef2da499842fb171a97f670d1841dc15a62ac52`.

Read the master, R6A through R6A-R4 contracts/results, complete two failed commits, all 127 current prompts, production validator/tests, prompt-audit evidence, and generated plan before editing. Preserve history and close every R6A-R4 blocker exactly.

## Zero-credit boundary and writable paths

No Meshy call, credential access, or network probe. Writable only: canonical leaf spec; generated plan/cost artifacts; `leafSpec.mjs`; `build-plan.mjs`/`plan.mjs` only where necessary for fail-closed library enforcement; relevant leaf/plan tests; and new `docs/meshy-production/evidence/MESHY-002-R6A-R5/**`. Do not edit any prior evidence/review/task/ledger/manifest/app/Xcode/agent file.

## Required prompt corrections

Rewrite these eight prompts using their own absolute subject, dimensions/proportions, construction, material, finish, placement, and purpose—no cross-asset comparison:

- `paper.pp_garden.shop` and `.gameplay`: describe ivy confined to a numeric outer margin and a plain central playing field; do not use “clear of,” “leave/leaving,” “free,” or another exclusion.
- `env.shopWall.nearBook.body`: give an absolute book height/width/depth and saturated binding/foreground placement; never mention receding books.
- `env.shopWall.recedingBook.body`: give an absolute compact height/width/depth and muted binding/back-shelf placement; never mention near books.
- `env.floor.seam`: give an absolute groove width/depth/orientation; never compare to the slab.
- `shop.sign.board`: give absolute width/height/depth and mounting position; never mention the notice board.
- `shop.lamp.softLightTrace`: give absolute cone angle/length/opacity/falloff; never mention the primary beam.
- `effect.nb_flame.embers`: give absolute ember size/count/intensity/placement; never mention the flame crown.

Re-read all 127 again and remove any similar construct found.

## Required enforcement

1. Treat every submittable prompt as invalid on whole-word/phrase negative or exclusion constructs, including no/not/never/without/unlike/except/cannot; can't/isn't/doesn't/aren't/won't (straight and curly apostrophes); distinct/different/separate/unrelated/independent/other than/rather than/instead of; avoid/exclude/omit/lack/absent/free of/clear of/away from; negative `non-*`; compare/compared/versus/vs/opposite; and bare whole-word `than`. Ban euphemistic cross-asset references detected by the audit. Preserve harmless substrings such as notice, nonetheless, exceptional, avoidance, and canonical.
2. Move `cannot` out of positive controls into adversarial required detection. Add both apostrophe forms for all contractions. Add poisoned real-spec fixtures for `clear of`, `cannot`, `aren't`, `won't`, `omit`, `lack`, `absent`, `free of`, `away from`, bare `than`, and a named other asset.
3. Enforce validation in the underlying library path used by `buildGenerationPlan`, not only the outer CLI script, so a direct import cannot generate a plan from poisoned specs. CLI remains fail-closed too.
4. Do not call any author-written scan “independent.” New author evidence may be labeled secondary/self-check only. True independence comes from the next `sonnet-independent-reviewer` run. Correct any overclaim in new evidence without rewriting old evidence.
5. Regenerate leaf spec/plan/cost and a complete before/after audit deterministically. Preserve 208/order/206+2/127/79 counts, 127 unique bodies/hashes, 1–600 chars, endpoints, references, and costs.

## Gates and delivery

Full prior 287 tests plus new adversarial/direct-library tests; an independently structured secondary scan with a superset vocabulary; deterministic double builds; syntax/JSON/schema/cross-reference/hashes/redaction/allowlist/`git diff --check`; clean worktree and protected root. Evidence explicitly cites both prior FAILs. Create exactly one clean Sonnet-authored child commit of the exact parent. Return `COMPLETE_FOR_REREVIEW` only if all gates pass; otherwise `PARTIAL`. Sections: STATUS, AGENT_RUN_ID, CHANGED_FILES, COMMIT, EIGHT_PROMPT_CORRECTIONS, PRODUCTION_ENFORCEMENT, TESTS_AND_GATES, EVIDENCE_INTEGRITY, ZERO_CREDIT_PROOF, CHECKSUMS, LIMITATIONS, BLOCKERS. Report the run ID exactly and leave canonical-model verification to the manager.
