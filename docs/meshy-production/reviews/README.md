# Independent review reports

Each accepted production task requires a separate read-only
`sonnet-independent-reviewer` report in this directory. Reports contain the
review run ID, exact resolved Sonnet model, author run/commit, requirement-level
pass/fail, blocking and non-blocking findings, file/symbol and screenshot
references, affected ledger rows, remediation, and re-review result.
